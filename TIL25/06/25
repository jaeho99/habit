class HelloWorld {
  static int[] MakeArray() {
  int[] tempArr = new int[4];

    for(int i = 0; i < tempArr.length; i++) {
      tempArr[i] = i;
    }

    return tempArr;
  }

  public static void main(String[] args) {
    int[] intArr;
    intArr = MakeArray();

    for(int i = 0; i < intArr.length; i++) {
      System.out.print(intArr[i]);
    }
  }
}

해석
배열 생성 클래스에서 메서드를 통해 4개의 길이 만큼 배열 tempArr 생성
for문으로 배열의 길이만큼 반복 4 (0, 1, 2, 3)
tempArr[0] = 0 ~ tempArr[3] = 3
담긴 배열 리턴

메인for문에서 arr 크기보다 작게 반복
arr 크기는 4 i 는 0부터 3까지 이므로 0, 1, 2, 3 반복

intArr[0] = 0 이다 순서대로 3까지 반복된다
출력은 println이 아니므로 줄바꿈이 없고 띄어쓰기도 존재하지 않는다.

출력
0123
